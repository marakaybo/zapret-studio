# Сборочный мусор. Больше всего весит target: 11 ГБ промежуточных объектов
# Rust, которые полностью восстанавливаются из исходников
/src-tauri/target/
/target-next/
/node_modules/
/dist/

# Настройки самого приложения живут в %APPDATA%, но если кто-то запустит
# портативно — пусть не улетят в репозиторий: внутри ссылки на сервер с uuid
config.json

# Локальные мелочи
.DS_Store
Thumbs.db
*.log

# Схемы разрешений Tauri генерируются при сборке
/src-tauri/gen/schemas/
